let counter = 0;
console.time("performance"); //Grava o tempo em milisegundo.
while(counter < 100000) {
    "JavaScript";
    counter++;
}
console.timeEnd("performance")// Determina qunato tempo passou entre o início e o fim.

//Para fazer uma declaração entre aspas dentro de uma string, precisamos colocar o trecho entre barras invertidas no início e antes do fechamento das aspas. Ex.: \"vulgo Alemão\"

//Para quebrar a linha dentro de uma string, utilizamos mais uma barra invertida antes do n. Ex.: \\n